---
description: Open Serial Monitor to view logs from ESP32-C3
---

## Steps
1. ตรวจสอบว่าต่อสาย USB-C เข้าบอร์ดแล้ว
2. รันคำสั่ง:
```bash
cd c:/MCU/ESP32/ESP32_C3/check_wifi && pio device monitor -b 115200
```
3. กด `Ctrl+C` เพื่อหยุด Monitor
